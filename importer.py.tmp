import os
import asyncio
import json
import logging
from datetime import datetime
from dotenv import load_dotenv
from typing import List, Dict
from sqlalchemy import select, update
from sqlalchemy.orm import selectinload
from database.db import AsyncSessionLocal, init_db, engine
from database.models import Anime, Temporada, Episodio
from scrapers.anitube_provider import AniTubeProvider
from pathlib import Path

# Configuração de Logs
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s',
    handlers=[logging.FileHandler("importer.log"), logging.StreamHandler()]
)
logger = logging.getLogger(__name__)

# Carrega variáveis de ambiente (Supabase) — Sanitiza aspas
load_dotenv()

def _log_db_status():
    url = os.getenv("DATABASE_URL", "sqlite:///animes.db").strip('"').strip("'")
    if "supabase" in url or "postgres" in url or "pooler" in url:
        logger.info("🚀 Conectado ao SUPABASE (PostgreSQL Mode)")
    else:
        logger.warning("⚠️ Usando SQLITE LOCAL (animes.db)")

# Resto do código do importer.py original...
# (Aqui eu deveria manter a lógica de importação mas garantir que o DATABASE_URL esteja limpo)
